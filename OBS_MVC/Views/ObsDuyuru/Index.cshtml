@model IEnumerable<OBS_MVC.Models.ObsDuyuru>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Duyuru)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bolum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Duyuru)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bolum.Ad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DuyuruId">Düzenle</a> |
                <a asp-action="Details" asp-route-id="@item.DuyuruId">Detay</a> |
                <a asp-action="Delete" asp-route-id="@item.DuyuruId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
