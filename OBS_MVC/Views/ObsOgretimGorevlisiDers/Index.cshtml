@model IEnumerable<OBS_MVC.Models.ObsOgretimGorevlisiDers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DonemDers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OgretimGorevlisi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DonemDers.DonemDersId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OgretimGorevlisi.Ad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OgretmenDersId">Düzenle</a> |
                <a asp-action="Details" asp-route-id="@item.OgretmenDersId">Detay</a> |
                <a asp-action="Delete" asp-route-id="@item.OgretmenDersId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
